name: Cloud Model Trainer

on:
  push:
    paths:
      - 'models/engine.py'
  workflow_dispatch: 

jobs:
  build-and-train:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true # Ensures LFS is ready

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements_train.txt

      - name: Train Deep Learning Models
        run: python train_models.py

      - name: Commit and Push Model Files
        run: |
          # 1. Identity Setup
          git config --global user.name 'GitHub Action Bot'
          git config --global user.email 'action@github.com'
          
          # 2. Initialize Git LFS
          git lfs install
          
          # 3. Track .h5 files as Large Files
          # This creates/updates a .gitattributes file
          git lfs track "models/*.h5"
          git add .gitattributes
          
          # 4. Add the model files
          git add models/*.h5
          
          # 5. Commit and Push
         - name: Commit and Push Model Files
        run: |
          # 1. Setup Identity
          git config --global user.name 'GitHub Action Bot'
          git config --global user.email 'action@github.com'
          
          # 2. Install and Initialize Git LFS
          # This is required by Hugging Face for .h5 files
          git lfs install
          
          # 3. Track .h5 files as Large Files
          # This creates/updates the .gitattributes file
          git lfs track "models/*.h5"
          git add .gitattributes
          
          # 4. Add the model files
          git add models/*.h5
          
          # 5. Commit and Push
          # We use --force to overwrite any remote conflicts caused by the binary rejection
          git commit -m "🤖 Auto-update neural weights via LFS" || echo "No changes"
          git push --force
